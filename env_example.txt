DATABASE_URL=postgresql+psycopg2://<username>:<password>@localhost:5432/<dbname>
BACKEND_API_URL=http://localhost:8000/api

# Security settings (for FastAPI JWT and Flask Session)

SECRET_KEY=dev-secret-key-replace-in-production
JWT_ALGORITHM=HS256

# Aadhar protection keys - base64, 32 bytes each. NEVER store these in the DB.
# Generate: python -c "import backend.utils.aadhar_crypto as a; print(a.generate_keys())"
AADHAR_HMAC_KEY=
AADHAR_ENC_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=360

# SMTP settings for fastapi-mail
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_FROM=your_email@gmail.com
MAIL_PORT=587
MAIL_SERVER=smtp.gmail.com
MAIL_FROM_NAME="CHiPS Admin Portal"
ENABLE_LANGUAGE_TOGGLE=True
UIDAI_RECIPIENT_EMAIL=recipient_email@example.com
